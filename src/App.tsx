import React, { Fragment } from 'react';
import TodoList from './pages/TodoList';

const App = (): JSX.Element => {
  return (
    <Fragment>
      <TodoList></TodoList>
    </Fragment>
  );
};

export default App;
